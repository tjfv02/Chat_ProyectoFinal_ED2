@model IEnumerable<DiseñoChatMVC.Models.Mensaje>

@{
    ViewBag.Title = "Chat";
}

<h2>Chat</h2>

<p>
    @Html.ActionLink("Enviar Mensaje", "Mensajeria")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label(" ")
        </th>
        <th>
            @Html.Label("Mensaje")
        </th>

        <th>
            @Html.Label("Destino")
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Emisor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contenido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoraActual)
            </td>
           
        </tr>
    }

</table>

